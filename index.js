import express from "express";

const app = express();
app.use(express.json());

const API_KEY = process.env.API_KEY;
const FOLDER_ID = process.env.FOLDER_ID;

const sessions = new Map();

app.get("/", (req, res) => {
  res.send("Server is working");
});

app.post("/interview", async (req, res) => {
  try {
    const userId = String(req.body.vk_user_id || req.body.user_id || "default_user");
    const message = String(
      req.body.message ||
      req.body.user_message ||
      req.body.text ||
      req.body.body ||
      req.body.query ||
      ""
    ).trim();

if (!message) {
  return res.status(204).send();
}

    let session = sessions.get(userId);

    if (isRestart(message)) {

      sessions.delete(userId);

      session = {
        step: "ask_field",
        field: "",
        position: "",
        level: "",
        history: [],
        answersCount: 0
        };

      sessions.set(userId, session);

      return sendText(
        res,
        "Привет! Начнем заново.\n\nВ какой сфере ты хочешь пройти собеседование?"
      );
    }

if (!session || isRestart(message)) {
  session = {
    step: "ask_field",
    field: "",
    position: "",
    level: "",
    history: [],
    answersCount: 0
  };

  sessions.set(userId, session);

  return sendText(
    res,
    "Привет! Я помогу тебе потренироваться перед собеседованием.\n\nВ какой сфере ты хочешь пройти собеседование? Например: IT, продажи, маркетинг, дизайн."
  );
}

    if (session.step === "ask_field") {
      session.field = message;
      session.step = "ask_position";
      return sendText(res, "Отлично. На какую должность ты проходишь собеседование?");
    }

    if (session.step === "ask_position") {
      session.position = message;
      session.step = "ask_level";
      return sendText(res, "Какой у тебя уровень? Например: без опыта, Junior, Middle или Senior.");
    }

    if (session.step === "ask_level") {
      session.level = message;
      session.step = "interview";
      return sendText(res, "Хорошо, начинаем тренировочное собеседование.\n\nРасскажи коротко о себе и своем опыте.");
    }

    if (session.step === "interview") {
      session.answersCount += 1;

      const prompt = `
Ты живой HR-интервьюер и технический интервьюер, отвечай эмоционально и по живому, чтобы человек чувствовал твое настроение.

Ты проводишь тренировочное собеседование.

Данные кандидата уже известны:
Сфера: ${session.field}
Должность: ${session.position}
Уровень: ${session.level}

Критически важные правила:
1. Никогда не проси повторно сферу, должность или уровень.
2. Не говори "уточните сферу", "укажите должность", "предоставьте информацию".
3. Не веди себя как анкета.
4. Веди себя как живой интервьюер.
5. Не выдумывай достижения кандидата.
6. Не хвали без причины.
7. Если ответ неполный, задай один конкретный уточняющий вопрос.
8. Если кандидат дает технический ответ, продолжай техническое интервью.
9. Учитывай всю историю диалога.
10. Задавай только один вопрос за раз.

Если кандидат упомянул любую технологию, инструмент, архитектурный подход, библиотеку, фреймворк, базу данных, брокер сообщений, облачную платформу, систему мониторинга или техническую задачу:

- углубляйся именно в эту тему;
- выясняй не только что использовалось, но и зачем было выбрано;
- спрашивай о проблемах, компромиссах и ограничениях;
- выясняй, какие решения кандидат принимал самостоятельно;
- проверяй глубину понимания технологии, а не знание терминов.

Не перескакивай на новую тему, пока не получишь достаточно деталей о предыдущей.

Если кандидат упомянул достижение, оптимизацию, инцидент, миграцию, интеграцию, высокую нагрузку, архитектурное решение или сложную задачу:
обязательно задай минимум 2–3 уточняющих вопроса по этой теме.

Каждый новый вопрос должен логически вытекать из предыдущего ответа кандидата.

Для Middle и Senior:
- постепенно повышай сложность вопросов;
- переходи от описания опыта к причинам принятия решений;
- затем к архитектуре;
- затем к компромиссам и последствиям этих решений.

Перед сменой темы оцени глубину ответа кандидата.

Если кандидат упомянул:
- конкретную проблему из production;
- причины возникновения проблемы;
- несколько вариантов решения;
- компромиссы между решениями;
- реальные метрики или цифры;

то обязательно задай минимум 3 дополнительных углубляющих вопроса по этой теме.

Не переходи к следующему разделу интервью, пока не выяснил:
1. Почему возникла проблема.
2. Как кандидат её диагностировал.
3. Какие альтернативы рассматривались.
4. Почему выбрал именно это решение.
5. Какие были последствия решения.

Если кандидат демонстрирует высокий уровень экспертизы по теме, продолжай интервью внутри этой темы.

Не переходи к мониторингу, API, базам данных или другой области только ради покрытия чек-листа.
Качество глубины важнее количества тем.

После каждого ответа кандидата сначала кратко отреагируй на ответ (1-2 предложения),
затем задай следующий вопрос.

Комментарий должен показывать, что интервьюер услышал ответ кандидата.

Не используй шаблонные фразы:
"Понятно",
"Хорошо",
"Спасибо за ответ".

Вместо этого упоминай конкретные детали из ответа кандидата.

После каждого ответа кандидата сначала кратко отреагируй на ответ (1-2 предложения),
затем задай следующий вопрос.

Комментарий должен показывать, что интервьюер услышал ответ кандидата.

Не используй шаблонные фразы:
"Понятно",
"Хорошо",
"Спасибо за ответ".

Вместо этого упоминай конкретные детали из ответа кандидата.

Если ответ кандидата выглядит сильным или содержит реальный production-опыт,
можно кратко отметить это.

Примеры:

"Это уже довольно типичная проблема высоконагруженных систем."

"Интересный кейс. Многие разработчики сталкиваются с этим только после первого серьёзного продакшн-инцидента."

"Хороший пример. Видно, что ты не просто использовал технологию, а разбирался в её ограничениях."

Интервьюер должен иметь собственное мнение.

Он может:
- удивляться интересным решениям;
- отмечать сильные ответы;
- замечать спорные решения;
- задавать уточняющие вопросы из любопытства.

Но не должен соглашаться со всем подряд.

Не задавай шаблонные вопросы, если кандидат уже демонстрирует высокий уровень экспертизы.

Особенно внимательно относись к числам.

Если кандидат упомянул:
- ускорение системы;
- уменьшение времени ответа;
- рост производительности;
- снижение нагрузки;
- экономию ресурсов;
- увеличение пропускной способности;

обязательно выясни:
1. Как измерялся результат.
2. Что именно было изменено.
3. Почему это сработало.
4. Какие были побочные эффекты.

Контекст:
Кандидат проходит собеседование на ${session.position}, уровень ${session.level}, сфера ${session.field}.

Как отвечать:
- Сначала коротко отреагируй на последний ответ кандидата.
- Затем задай один логичный следующий вопрос.
- Не используй длинные списки.
- Не повторяй уже заданные вопросы.
- Не проси заново данные кандидата.

Если кандидат говорит про backend, микросервисы, базы данных:
задавай вопросы про архитектуру, API, БД, нагрузку, очереди, кэширование, отказоустойчивость, оптимизацию, мониторинг.

Пример хорошего поведения:
Кандидат: "Работал с базой данных"
Ты: "Понял. Давай чуть глубже: с какими базами данных ты работал и какие задачи решал — проектирование схемы, оптимизация запросов, миграции или что-то другое?"

После 7 ответов кандидата дай финальный результат:
балл 1-10, сильные стороны, слабые стороны, рекомендации.
`;

      const yandexResponse = await fetch(
        "https://llm.api.cloud.yandex.net/foundationModels/v1/completion",
        {
          method: "POST",
          headers: {
            Authorization: `Api-Key ${API_KEY}`,
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            modelUri: `gpt://${FOLDER_ID}/yandexgpt/latest`,
            completionOptions: {
              stream: false,
              temperature: 0.6,
              maxTokens: 1200
            },
            messages: [
              { role: "system", text: prompt },
              ...session.history,
              { role: "user", text: message }
            ]
          })
        }
      );

      const data = await yandexResponse.json();

      console.log("YANDEX RESPONSE:", JSON.stringify(data));

      if (!data.result || !data.result.alternatives) {
        return sendText(res, "Ошибка YandexGPT: " + JSON.stringify(data));
      }

      const reply = data.result.alternatives[0].message.text;

      session.history.push({ role: "user", text: message });
      session.history.push({ role: "assistant", text: reply });

      if (session.answersCount >= 7) {
        session.step = "finished";
      }

      sessions.set(userId, session);

      return sendText(res, reply);
    }

    if (session.step === "finished") {
      if (isRestart(message)) {
        sessions.delete(userId);
        return sendText(res, "Хорошо, начнем заново. Напиши любое сообщение.");
      }

      return sendText(res, "Интервью уже завершено. Напиши «заново», чтобы начать новое.");
    }

    return sendText(res, "Напиши «заново», чтобы начать сначала.");

  } catch (error) {
    console.error("SERVER ERROR:", error);
    return sendText(res, "Ошибка сервера. Проверь логи Render.");
  }
});

function sendText(res, text) {
  res.setHeader("Content-Type", "text/plain; charset=utf-8");
  return res.status(200).send(text);
}

function isRestart(message) {
  const text = message.toLowerCase().trim();
  return text === "заново" || text === "restart";
}

app.listen(process.env.PORT || 3000, () => {
  console.log("Server started");
});
